namespace Player
{
    public enum ActionType
    {
        Left,
        Right,
        Down,
        Up,
        Drop,
        RotateClockwise,
        RotateCounterClockwise,
        Hold,
        Pause
    }
}